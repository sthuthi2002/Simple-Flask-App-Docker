apiVersion: v1
kind: Service
metadata:
  labels:
    app: monitorapp
  name: pythonmonitor
spec:
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: monitorapp
